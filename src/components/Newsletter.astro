---
import { Image } from "astro:assets";
import CardNewsletter from "@/components/CardNewsletter.astro";
import petLogo from "@/assets/images/mascota.png";
---

<section
    id="newsletter-section"
    class="mt-16 mb-16 px-4 sm:px-6 lg:px-8 relative overflow-hidden"
>
    <!-- Background decoration -->
    <div
        id="blob-bg"
        class="absolute -top-32 -right-20 w-auto h-96 bg-primary/10 rounded-full blur-3xl opacity-0"
    >
    </div>

    <h3
        id="newsletter-title"
        class="text-4xl md:text-6xl font-bold mb-8 text-center
           bg-linear-to-r from-primary to-secondary
           bg-clip-text text-transparent opacity-0 translate-x-[-50px]"
    >
        Mantente Actualizado
    </h3>

    <div
        id="newsletter-container"
        class="flex flex-wrap justify-around items-center gap-10 mt-20 opacity-0"
    >
        <div class="basis-full md:basis-1/3 lg:basis-1/2" id="newsletter-form">
            <CardNewsletter />
        </div>
        <div id="mascot-container" class="basis-auto translate-x-20">
            <Image
                src={petLogo}
                alt="mascota de guayaba"
                class="object-contain"
            />
        </div>
    </div>

    <!-- Toast container -->
    <div
        id="toast"
        class="fixed top-4 right-4 z-50 bg-green-500 text-white px-6 py-3 rounded-lg shadow-lg transform translate-y-[-100px] opacity-0 transition-all duration-300 flex items-center"
    >
        <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-5 w-5 mr-2"
            viewBox="0 0 20 20"
            fill="currentColor"
        >
            <path
                fill-rule="evenodd"
                d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                clip-rule="evenodd"></path>
        </svg>
        <span>Proximamente disponible ;)</span>
    </div>
</section>

<style>
    @keyframes bounceHorizontal {
        0%,
        100% {
            transform: translateX(0);
        }
        50% {
            transform: translateX(5px);
        }
    }

    .animate-bounce-horizontal {
        animation: bounceHorizontal 1s infinite;
    }
</style>
<script>
    import { gsap } from "@/utils/gsap-core";
    import { createIntersectionAnimation } from "@/utils/animation";

    document.addEventListener("DOMContentLoaded", () => {
        createIntersectionAnimation("#blob-bg", {
            opacity: 1,
            scale: 1.1,
            duration: 0.8,
        });

        createIntersectionAnimation("#newsletter-title", {
            opacity: 1,
            x: 0,
            duration: 0.8,
            ease: "back.out",
        });

        createIntersectionAnimation("#newsletter-container", {
            opacity: 1,
            duration: 0.8,
        });

        createIntersectionAnimation("#mascot-container", {
            opacity: 1,
            x: 0,
            duration: 0.8,
        });
    });

    const toast = document.getElementById("toast");
    if (toast) {
        document.addEventListener("showNewsletterToast", () => {
            // Show toast
            gsap.to(toast, {
                y: 100,
                opacity: 1,
                duration: 0.5,
                ease: "power3.out",
                onComplete: () => {
                    // Hide toast after 3 seconds
                    setTimeout(() => {
                        gsap.to(toast, {
                            y: -100,
                            opacity: 0,
                            duration: 0.5,
                            ease: "power3.in",
                        });
                    }, 3000);
                },
            });
        });
    }
</script>
